// === Components === //
import { Button } from 'antd'

const Home = () => {
    return <div>
        <p>Home</p>
        <Button type="primary">确认</Button>
    </div>
}
export default Home